import { Section } from "../enum/section.enum"

export class etudiant{
    id!: number
    name!: String
    surname!: String
    cin!: String
    classe!: String
    section!: Section
    average!: number

}